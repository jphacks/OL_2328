import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ol2328.app',
  appName: 'nottapple',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
