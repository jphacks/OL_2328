<!-- <script>
	import { Button } from "konsta/svelte";

    let filePickerElement: HTMLInputElement;

    const openDialog = () => {
        filePickerElement.click();
    };
</script>

<Button onClick={() => { openDialog(); }} rounded>
    
</Button>

<input bind:value={} bind:this={filePickerElement} class="invisible" type="file" /> -->